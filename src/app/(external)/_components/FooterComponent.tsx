import { cn } from "@/lib/utils";

const FooterBlock = ({ className }: { className: string | undefined }) => {
	return (
		//
		<footer className={cn("mx-auto mt-8 px-4 pt-2 bottom-0 inset-0", className)}>
			{/*  */}
			FooterBlock
			{/*  */}
		</footer>
	);
};

export default FooterBlock;
